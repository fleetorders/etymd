{
  "gates": {
    "commands": ["format:check", "typecheck", "test"],
    "commitFormat": true,
    "_why": {
      "commands": "Pinned so a pack regeneration cannot silently drop test — it is derived from the PREVIOUS hook, so deleting the hook loses it.",
      "commitFormat": "This repo keeps Conventional Commits, so it asks for the door. Off by default for everyone else — the pack ships the mechanism and never the opinion."
    }
  }
}
